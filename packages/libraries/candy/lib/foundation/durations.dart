// Speed
const fastAnimationDuration = Duration(milliseconds: 150);
const slowAnimationDuration = Duration(milliseconds: 1300);

// Situation
const standardAnimationDuration = Duration(milliseconds: 300);
const scaleAnimationDuration = Duration(milliseconds: 800);
